import { Schema, model } from "mongoose";
import { Enum_EstadoProyecto, Enum_FaseProyecto } from "./enums";

interface Proyect {
  // nombre: string;
  // presupuesto: ;
  // nombre: string;
  // apellido: string;
}